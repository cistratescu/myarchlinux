# myarchlinux
My Arch Linux installer

```bash
curl -Lo myarchlinux https://github.com/cistratescu/myarchlinux/archive/refs/heads/main.tar.gz && tar -xzf myarchlinux && rm -r myarchlinux && chmod +x myarchlinux-main/arch-install && ./myarchlinux-main/arch-install
```
